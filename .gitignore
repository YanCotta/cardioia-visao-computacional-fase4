# Credenciais — NUNCA commitar
kaggle.json
*.key
.env

# Dados (grandes / regeneráveis via Kaggle)
cardio_data/

# Modelos: VERSIONAMOS os finais (para rodar a demo/app sem re-treinar).
# Ignoramos apenas o intermediário (redundante) e formatos de peso soltos.
modelos/resnet50_fase1.keras
*.h5
*.weights.h5

# Node / Expo (app mobile)
node_modules/
.expo/
mobile/app/.expo/
npm-debug.log*

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# Sistema
.DS_Store
