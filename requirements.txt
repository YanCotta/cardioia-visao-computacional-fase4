# CardioIA - Fase 4 (Visão Computacional)
# Dependências do notebook. No Google Colab a maioria já vem instalada;
# instale manualmente apenas se rodar localmente.

tensorflow>=2.15
tf-keras            # Keras 2 legado (mantém ImageDataGenerator disponível no Keras 3)
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
pillow>=10.0
opencv-python>=4.8  # usado no overlay do Grad-CAM
gradio>=4,<5        # protótipo de interface (4.x: API allow_flagging usada no notebook)
huggingface_hub<1.0 # gradio 4.x importa HfFolder, removido no huggingface_hub 1.x
kaggle>=1.6         # download do dataset
